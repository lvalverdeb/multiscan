# Positive secrets fixtures — known true positives (commit-safe values only:
# the AWS documented example key, synthetic non-provider strings).

AWS_ACCESS_KEY_ID = "AKIAIOSFODNN7EXAMPLE"
DATABASE_URL = "postgres://svc:s0mePassw0rdValue@db.internal:5432/app"
password = "s0mePlaceholderValue123"

[package]
name = "multiscan-risk"
description = "Risk scoring formula and explanations. Pure — no I/O (spec 8)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
multiscan-core.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
